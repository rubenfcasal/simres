% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test.R
\name{chisq.cont.test}
\alias{chisq.cont.test}
\title{Chi-squared goodness-of-fit test for continuous data}
\usage{
chisq.cont.test(
  x,
  distribution = "norm",
  nclass = floor(length(x)/5),
  output = TRUE,
  nestpar = 0,
  ...
)
}
\arguments{
\item{x}{numeric vector containing the observed values.}

\item{distribution}{character string naming a continuous distribution,
such as "norm" or "unif" (the cumulative distribution \verb{p<distribution>()} and
quantile \verb{q<distribution>()} functions must exist).}

\item{nclass}{number of bins.}

\item{output}{logical; if \code{TRUE} an histogram is plotted and a table with
the results for each class is printed.}

\item{nestpar}{number of estimated parameters (composite null hypothesis).}

\item{...}{parameters of the distribution (specified by \code{distribution}).}
}
\value{
A list with class \code{"htest"} containing the following components:
\item{statistic}{the value the chi-squared test statistic.}
\item{parameter}{the degrees of freedom of the approximate chi-squared
distribution of the test statistic.}
\item{p.value}{the p-value for the test.}
\item{method}{a character string indicating the type of test performed.}
\item{data.name}{a character string with the actual \code{x} argument name.}
\item{classes}{a character vector with the class labels.}
\item{observed}{the observed counts.}
\item{expected}{the expected counts under the null hypothesis.}
\item{residuals}{the Pearson residuals, \code{(observed - expected) / sqrt(expected)}.}
}
\description{
Performs the chi-squared goodness-of-fit test for a continuous distribution
by grouping data into \emph{bins} (also called \emph{intervals}, \emph{classes} or \emph{cells})
with equal probabilities under the null hypothesis.
}
\examples{
nx <- 30
x <- rnorm(nx)
chisq.cont.test(x, distribution = "norm", nestpar = 2,
                mean = mean(x), sd = sqrt((nx - 1) / nx) * sd(x))
}
\seealso{
\code{\link{chisq.test}} \code{\link{freq.test}}
}
